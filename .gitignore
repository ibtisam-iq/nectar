# MkDocs
site/
.cache/

# Python
*.pyc
__pycache__/
.venv/
venv/
*.egg-info/

# macOS
.DS_Store

*.bak
.scratchpad/

.claude/
# Raw source material for tool folders (never published).
# INVENTORY.md is force-tracked (git add -f) so the completion audit (check 9)
# can run on a fresh checkout; everything else under _sources/ stays ignored.
_sources/
